export default {
    setUserId(state, userId) {
        state.userId = userId
    },
    setUsername(state, userName) {
        state.userName = userName
    },
    setToken(state, token) {
        state.token = token;
    }
}
